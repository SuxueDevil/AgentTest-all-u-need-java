# 5.17 开发计划

## 当前进度概览
QuestionGenerator 参数 DTO 化重构 + 前端优化启动

## 已完成事项
- [x] 新增 QuestionGenerateDTO（aun-agent 模块），封装 category/difficulty/questionType/count/topic
- [x] QuestionGenerator.generate() 参数从 5 个散参数改为接收 QuestionGenerateDTO
- [x] QuestionService.generate() 接口签名同步更新
- [x] QuestionServiceImpl.generate() 适配 DTO
- [x] QuestionController.generate() 从 Map<String, Object> 手动取值改为 @RequestBody QuestionGenerateDTO 自动绑定

## 下一步待做功能
- [ ] 前端优化（Phase 5）
