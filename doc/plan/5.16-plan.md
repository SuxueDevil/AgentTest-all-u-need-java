# 5.16 开发计划

## 当前进度概览
日志系统持久化 — 补齐 logback-spring.xml + Dockerfile，让生产环境可查日志。

## 已完成事项
- [x] 新增 logback-spring.xml（console + file appender → logs/app.log，按天+大小滚动，30天保留）
- [x] MyBatis-Plus 日志实现 StdOutImpl → Slf4jImpl（走 Logback 统一管理）
- [x] 新增后端 Dockerfile（多阶段构建：Maven 3.9 + JDK 17 build → JRE 17 run）
- [x] docker-compose.yml 后端容器挂载 `./logs:/app/logs`
- [x] 新增 RequestLogInterceptor（请求日志拦截器，显示 → GET /path | Controller.method）
- [x] 新增 WebMvcConfig（注册拦截器）
- [x] AgentServiceImpl + QuestionServiceImpl 新增中文业务日志（分页/详情/创建/更新/删除）

## 下一步待做功能
- [ ] Phase 3.2: 评测任务后端 — EvaluationTask + EvaluationResult CRUD（7 个 API）
- [ ] Phase 3.3: 评测引擎 — AgentHttpClient + RequestBuilder + 评分

## API / 接口清单
（本次无新增接口）

## 阻塞 / 风险
- Judge LLM 选型待定（评分引擎需要）
- 多轮对话消息格式待定
