# 5.16 开发计划

## 当前进度概览
日志系统完善 + Phase 3.2~3.3 评测任务后端全部完成。Phase 3 整体收官。

## 已完成事项

### 日志系统
- [x] logback-spring.xml（console + file → logs/app.log，按天+大小滚动，30天保留）
- [x] MyBatis-Plus 日志实现 StdOutImpl → Slf4jImpl
- [x] Dockerfile + docker-compose logs 挂载
- [x] RequestLogInterceptor（分隔线 + Controller 类名 + 耗时）+ WebMvcConfig
- [x] AgentServiceImpl + QuestionServiceImpl 中文业务日志
- [x] AgentTestApplication 启动 banner + HikariCP 连接池预热

### Phase 3.2 评测任务后端
- [x] Entity: EvaluationTask + EvaluationResult（含 JacksonTypeHandler JSON 列）
- [x] DTO: CreateDTO / UpdateDTO / QueryDTO
- [x] VO: EvaluationTaskVO / TaskProgressVO / AgentResultVO（含逐题明细）
- [x] Mapper: EvaluationTaskMapper + EvaluationResultMapper
- [x] Service: EvaluationService + Impl — CRUD + start/cancel/progress/results
- [x] Controller: EvaluationController（9 个 REST 接口）

### Phase 3.3 评测引擎
- [x] AsyncConfig（@EnableAsync + evaluationExecutor 线程池 4→8）
- [x] AgentHttpClient（RestTemplate，支持 bearer/api_key/basic/custom，单轮/多轮 messages 组装）
- [x] ScoringEngine（占位实现，待替换 LLM-as-Judge）

## API / 接口清单
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/evaluation | 分页查询任务列表 |
| GET | /api/evaluation/{id} | 任务详情 |
| POST | /api/evaluation | 创建评测任务 |
| PUT | /api/evaluation/{id} | 更新任务 |
| DELETE | /api/evaluation/{id} | 删除任务 |
| POST | /api/evaluation/{id}/start | 启动异步评测 |
| POST | /api/evaluation/{id}/cancel | 取消评测 |
| GET | /api/evaluation/{id}/progress | 进度轮询 |
| GET | /api/evaluation/{id}/results | 结果查询（按 Agent 分组） |

## 下一步待做功能
- [ ] Phase 4: 报告 + 仪表盘
- [ ] Judge LLM 替换 ScoringEngine 占位实现

## 阻塞 / 风险
- Judge LLM 选型待定（当前 ScoringEngine 为启发式占位，准确度不足）
