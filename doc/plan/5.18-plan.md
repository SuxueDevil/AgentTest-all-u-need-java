# 5.18 开发计划

## 当前进度概览
修复多轮对话评测的核心 bug：assistant 消息泄露 + 逐轮迭代发送 + rawRequest 双重序列化。重构 AgentHttpClient 抽取核心 HTTP 方法。

## 下一步待做功能
- [ ] Phase 4 — 报告 + 仪表盘
- [ ] 架构.md 更新
- [ ] 现有数据库 ALTER TABLE 加 turn_order 列（新库走 init.sql 已包含）

## API / 接口清单
无新增接口，仅修复现有逻辑。

## 已完成事项
- [x] AgentHttpClient 重构：抽取 execute() / executeLLM() 核心方法
- [x] AgentHttpClient 新增 sendMultiTurn() — 逐轮迭代发送 user 消息，用实际 LLM 回答构建上下文
- [x] AgentHttpClient 新增 sendMultiTurnToLLM() — LLM 多轮迭代版本
- [x] buildMessages 过滤 role=assistant，只发 user 消息给被测 LLM
- [x] EvaluationResult 实体新增 turnOrder 字段（多轮轮次序号，1-based）
- [x] init.sql evaluation_result 表新增 turn_order / agent_response 列
- [x] AgentResultVO.ResultItemVO 新增 turnOrder 字段
- [x] EvaluationServiceImpl.create() — questionCount 按 user 消息数计算
- [x] EvaluationServiceImpl.executeAsync() — 多轮题目走 sendMultiTurn，每轮独立 Judge 评分
- [x] EvaluationServiceImpl 注入 ObjectMapper，新增 parseJsonSafely() 方法
- [x] rawRequest 设值前解析 JSON 字符串为 Map/List，避免 JacksonTypeHandler 双重序列化

## 阻塞 / 风险
- [ ] 现有运行中的数据库需手动 ALTER TABLE evaluation_result ADD COLUMN turn_order INT, ADD COLUMN agent_response TEXT
