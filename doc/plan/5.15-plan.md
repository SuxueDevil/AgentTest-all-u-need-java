# 5.15 开发计划

## 当前进度概览
Phase 2 全部完成 — Question 模块 + application 配置优化。下一步 Phase 3.2 评测任务后端。

## 已完成事项
- [x] 项目初始化: pom.xml（Spring Boot 3.3.5 + MyBatis-Plus 3.5.7 + MySQL + Hutool + RestTemplate）
- [x] application.yml: 端口 8080, 数据源配置, Jackson 日期格式
- [x] docker-compose.yml: MySQL 8.0 + 后端 + 前端
- [x] init.sql: 5 张表建表脚本 + 25 条预置样本数据（20 单轮 + 5 多轮）
- [x] 公共层: ApiResponse, PageResult, BusinessException, GlobalExceptionHandler
- [x] 配置: CorsConfig, RestTemplateConfig (10s连接/30s读取), MyBatisPlusConfig (分页插件)
- [x] Agent 模块: entity, mapper, service, controller (6 个接口)
- [x] Agent 连接测试: 支持 bearer/api_key/basic/custom 四种鉴权方式
- [x] Agent 精简: 移除 tags 字段（entity / DTO / VO / init.sql）
- [x] 新增 custom 鉴权: authCredential 填 JSON `{"header":"value"}` 即可自定义任意 Header
- [x] application.yml: 数据源改 ${DB_HOST}:${DB_PORT}/${DB_USERNAME}/${DB_PASSWORD}，加 spring.profiles.active: dev
- [x] application-example.yml: 新增本地配置模板（已提交），application-dev.yml gitignore
- [x] fix: init.sql 样本数据 turn_order → turnOrder，Question.Turn 加 @JsonAlias 兼容

## API / 接口清单
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/agents | 分页查询（keyword, type, status, page, pageSize） |
| GET | /api/agents/{id} | 详情 |
| POST | /api/agents | 创建 |
| PUT | /api/agents/{id} | 更新 |
| DELETE | /api/agents/{id} | 删除 |
| POST | /api/agents/{id}/test-connection | 测试连接（向 endpoint 发送 ping） |

## 下一步待做功能
- [ ] Phase 3.2: 评测任务后端 — EvaluationTask + EvaluationResult CRUD（9 个 API）
- [ ] Phase 3.3: 评测引擎 — AgentHttpClient + RequestBuilder + 评分

## 新增 API（题库管理）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/questions | 分页查询（keyword, category, difficulty, questionType, page, pageSize） |
| GET | /api/questions/{id} | 详情 |
| POST | /api/questions | 创建（含多轮 turns 数组） |
| PUT | /api/questions/{id} | 更新 |
| DELETE | /api/questions/{id} | 删除 |
| DELETE | /api/questions/batch | 批量删除（请求体为 ID 数组） |
| POST | /api/questions/import | CSV/JSON 文件导入（multipart/form-data） |
| GET | /api/questions/export?format=json | 导出 CSV/JSON 文件 |

## 技术决策记录
- HTTP 调用使用 Spring Boot 自带的 RestTemplate（用户指定，非 OkHttp）
- 鉴权凭证明文存储（MVP 阶段不做加密）
- 使用 MyBatis-Plus 构造器模式而非 XML
